# KnowLever Open — Python dependencies for Stage 2 (Embed + Cluster)
# Install: pip install -r requirements.txt

numpy>=1.24
scikit-learn>=1.3
requests>=2.28

# Optional but recommended for better clustering:
hdbscan>=0.8.33
umap-learn>=0.5.4
scipy>=1.10
